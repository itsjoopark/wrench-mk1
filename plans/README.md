# Animation improvement plans

Generated by `improve-animations` at commit `95c6e4b`.

Audit summary: Wrench has almost no deliberate motion today — four `transition-colors` usages and instant state swaps. Nothing is actively wrong (no `ease-in`, `scale(0)`, or `transition: all`), but high-traffic interactions lack press feedback, spatial continuity, and shared tokens.

## Plans

| # | Title | Severity | Status | Depends on |
| --- | --- | --- | --- | --- |
| 001 | Add shared motion tokens | MEDIUM | DONE | — |
| 002 | Press feedback on pills and CTAs | MEDIUM | DONE | 001 |
| 003 | Animate anatomy part selection | HIGH | DONE | 001 |
| 004 | Mode switch crossfade | MEDIUM | DONE | 001, 003 (shared keyframe) |
| 005 | Gate hover for touch | MEDIUM | DONE | — |

## Recommended execution order

1. **001** — Foundation tokens and reduced-motion baseline (everything else references these).
2. **005** — Independent accessibility fix; can run in parallel with 001.
3. **002** — Quick win on the most-tapped controls.
4. **003** — Highest UX leverage for anatomy mode.
5. **004** — Mode crossfade (reuses `fade-in` from 003).

## How to execute

Tell any agent:

```
improve-animations execute plans/001-motion-tokens.md
```

Or paste a plan file as context and ask it to implement exactly.

After each plan, run `review-animations` on the diff before merging.

## Deferred findings (not planned)

| Severity | Finding | Why deferred |
| --- | --- | --- |
| LOW | Phase transitions (upload → detecting → ready) teleport | Occasional; lower leverage than part selection |
| LOW | Repair plan / answer text appear instantly | Occasional; additive polish |
| LOW | Consolidate implicit `transition-colors` durations | Covered once tokens exist and components are touched |
